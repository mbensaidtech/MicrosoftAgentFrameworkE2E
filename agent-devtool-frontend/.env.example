# Backend API URL - point to your backend
VITE_API_BASE_URL=http://localhost:5016

# Optional: Enable debug panel
VITE_ENABLE_DEBUG=true

# Optional: Default protocol (rest or a2a)
VITE_DEFAULT_PROTOCOL=rest

# Optional: Enable CORS proxy (if backend doesn't support CORS)
VITE_USE_PROXY=false
